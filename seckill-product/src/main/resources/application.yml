server:
  port: 8083  # 商品服务端口

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/seckill_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl